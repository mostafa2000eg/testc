# متطلبات نظام إدارة مشاكل العملاء - النسخة المحسنة
# Customer Issues Management System Requirements - Enhanced Version

# المكتبات الأساسية (Essential Libraries)
# هذه المكتبات مطلوبة لتشغيل النظام
sqlite3  # مدمجة مع Python - قاعدة البيانات

# المكتبات الاختيارية للتحسينات (Optional Enhancement Libraries)
# يمكن تثبيتها لتحسين الوظائف

# تقارير PDF المتقدمة
reportlab>=3.6.0

# معالجة الصور
Pillow>=8.0.0

# وظائف ويندوز المتقدمة (للويندوز فقط)
pywin32>=301; sys_platform == "win32"

# تحسين الواجهة الرسومية
# تتوفر هذه المكتبات كبدائل لـ tkinter إذا رغبت في ترقية الواجهة لاحقاً
# PyQt5>=5.15.0  # بديل متقدم لـ tkinter (اختياري)
# customtkinter>=5.0.0  # واجهة tkinter محسنة (اختياري)

# مكتبات أخرى مفيدة
python-dateutil>=2.8.0  # تحسين التعامل مع التواريخ
openpyxl>=3.0.0  # تصدير إلى Excel (اختياري)

# ملاحظات التثبيت:
# 1. المكتبات الأساسية (sqlite3, tkinter) مدمجة مع Python ولا تحتاج تثبيت
# 2. يمكن تثبيت المكتبات الاختيارية باستخدام:
#    pip install -r enhanced_requirements.txt
# 3. النظام سيعمل بدون المكتبات الاختيارية لكن مع وظائف أقل
# 4. في ويندوز، pywin32 يوفر وظائف إضافية لفتح الملفات والمجلدات